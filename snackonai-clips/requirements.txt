# ── Core content extraction ────────────────────────────────────────────────
trafilatura>=1.9.0          # Preferred HTML → text extractor
readability-lxml>=0.8.1     # Fallback extractor
newspaper3k>=0.2.8          # Last-resort extractor

# ── HTTP ──────────────────────────────────────────────────────────────────
requests>=2.31.0

# ── LLM providers ─────────────────────────────────────────────────────────
openai>=1.35.0              # OpenAI API (GPT-4o-mini default)
# ollama                    # Uncomment if using Ollama Python SDK (optional)

# ── Video generation ──────────────────────────────────────────────────────
# NOTE: ImageMagick must be installed separately for TextClip (text rendering):
#   macOS:  brew install imagemagick
#   Ubuntu: sudo apt install imagemagick
#   Windows (Chocolatey): choco install imagemagick
moviepy==1.0.3              # Pin to 1.0.3 — stable API, 2.x has breaking changes
numpy>=1.24.0,<2.0.0        # moviepy 1.x is not compatible with numpy 2.x
Pillow>=10.0.0
imageio>=2.28.0             # moviepy backend for frame I/O
imageio-ffmpeg>=0.4.9       # bundled ffmpeg binary (no separate ffmpeg install needed)

# ── TTS ───────────────────────────────────────────────────────────────────
gTTS>=2.5.0                 # Free TTS
elevenlabs>=1.1.0           # Premium TTS (optional, requires API key)
mutagen>=1.47.0             # Audio file metadata (duration detection)

# ── CLI / UX ──────────────────────────────────────────────────────────────
rich>=13.7.0                # Beautiful terminal output & progress bars

# ── Testing ───────────────────────────────────────────────────────────────
pytest>=8.0.0
pytest-cov>=5.0.0
pytest-mock>=3.14.0
